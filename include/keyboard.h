#ifndef KEYBOARD_H
#define KEYBOARD_H

/* --- Прототипи функцій --- */

/**
 * Очікує введення рядка з клавіатури.
 * Відображає символи на екрані залежно від поточного режиму вікна.
 * @param buffer Буфер, куди запишеться рядок (нуль-термінований).
 */
void input_wait_string(char *buffer);

/**
 * Обробляє системні комбінації клавіш (хоткеї).
 * Змінює режими current_mode, show_crt_window тощо.
 * @param code Скан-код натиснутої клавіші.
 */
void handle_hotkeys(int code);

/* --- Зовнішні залежності --- */
/* Ці функції зазвичай реалізовані в асемблері або драйвері низького рівня */
extern int get_scancode();

#endif